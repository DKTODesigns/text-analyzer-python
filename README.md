# text-analyzer-python
Simple Python text analyzer for word frequency and preprocessing
# Text Analyzer (Python)

A simple Python project that demonstrates basic text analysis using object-oriented programming.

## Features
- Converts text to lowercase
- Removes punctuation
- Counts frequency of all words
- Counts frequency of a specific word

## Example

```python
text = "Lorem ipsum dolor! diam amet, consetetur Lorem magna."
analyzer = TextAnalyzer(text)

print(analyzer.fmtText)
print(analyzer.freqAll())
print(analyzer.freqOf("lorem"))
# Text Analyzer (Python)

A simple Python project that demonstrates basic text analysis using object-oriented programming.

## Features
- Converts text to lowercase
- Removes punctuation
- Counts frequency of all words
- Counts frequency of a specific word

## Example

```python
text = "Lorem ipsum dolor! diam amet, consetetur Lorem magna."
analyzer = TextAnalyzer(text)

print(analyzer.fmtText)
print(analyzer.freqAll())
print(analyzer.freqOf("lorem"))
Purpose

This project demonstrates how text can be preprocessed and analyzed to extract useful information such as word frequency.

Currently demonstrated using sample text (Lorem Ipsum), but can be extended to real-world data such as customer feedback or reviews.

Tech Used
Python
Object-Oriented Programming (OOP)
